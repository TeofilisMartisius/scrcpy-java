Provide Java bindings to scrcpy library. And a demo project to show it all in action.

I have provided PRs to convert scrcpy into a shared library. They are not accepted upstream yet, so
I'm temporarily providing relevant parts of scrcpy itself under scrcpy-lib/scrcpy